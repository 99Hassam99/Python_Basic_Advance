a = set() # create an empty set, don't use s={} as will create an empty dict
b = {} # it will create an empty dict not set

# Set is a collection of non-repetitive elements.
s = {1,2,3,2,4,3,2,4,1} # non-repetitive means
print(s) # return 1 2 3 4 only once

# sets are unorder, for order we need list